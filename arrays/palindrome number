class Solution(object):
    def isPalindrome(self, x):
        if x>=0:
            f=str(x)
            b=str(x)[::-1]
            if f==b:
                return True
            else:
                return False
        else:
            return False
